events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;
    
    sendfile on;
    keepalive_timeout 65;
    
    upstream django_app {
        server web:8000;
        keepalive 16;
    }
    
    server {
        listen 80;
        # ✅ Указываем реальный домен
        server_name tracking-orders.cubinez.ru www.tracking-orders.cubinez.ru;
        
        # Редирект с www на основной домен (опционально)
        if ($host = www.tracking-orders.cubinez.ru) {
            return 301 https://tracking-orders.cubinez.ru$request_uri;
        }
        
        location /static/ {
            alias /app/static/;
            expires 30d;
            access_log off;
            add_header Cache-Control "public, immutable";
        }
        
        location /media/ {
            alias /app/media/;
            expires 30d;
            access_log off;
        }
        
        location /admin/ {
            auth_basic "Admin Area - Authorization Required";
            auth_basic_user_file /etc/nginx/.htpasswd;
            
            proxy_pass http://django_app;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        
        location / {
            proxy_pass http://django_app;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        
        location /nginx-health {
            access_log off;
            return 200 "ok\n";
            add_header Content-Type text/plain;
        }
    }
    
    # ✅ Catch-all сервер для отклонения неизвестных доменов (защита от Host header attacks)
    server {
        listen 80 default_server;
        server_name _;
        return 444;  # Nginx: закрыть соединение без ответа
    }
}
